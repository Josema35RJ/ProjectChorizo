using UnityEngine;

namespace CMF
{
    // Cambia la herencia temporalmente a MonoBehaviour si no encuentra CharacterInput
    public class CharacterKeyboardInput : MonoBehaviour /* CharacterInput */
    {
        // Comenta el contenido interior que te dé errores
    }
}